<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0a0e1a">

    <!-- Status bar row: dot | log text | update timer | refresh button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#111827"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="4dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp">

        <!-- Connection dot: red = disconnected, green = connected -->
        <View
            android:id="@+id/conn_dot"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:background="@drawable/circle_dot"
            android:layout_marginEnd="7dp"/>

        <!-- Live log / status text -->
        <TextView
            android:id="@+id/status_bar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#00ff88"
            android:textSize="11sp"
            android:fontFamily="monospace"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="⚡ Supreme Bot — Initializing..."/>

        <!-- Countdown to next script check -->
        <TextView
            android:id="@+id/update_timer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#888888"
            android:textSize="10sp"
            android:fontFamily="monospace"
            android:text="🔄 5:00"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="2dp"/>

        <!-- Refresh page button -->
        <TextView
            android:id="@+id/btn_refresh"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="⟳"
            android:textColor="#00ff88"
            android:textSize="22sp"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Refresh page"/>

    </LinearLayout>

    <!-- Pull-to-refresh wrapping the WebView -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <WebView
            android:id="@+id/webview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</LinearLayout>
